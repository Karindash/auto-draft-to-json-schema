This form base apps made for visualize how we form database, create automated SQL/DDL due to our design, transcribe it into multiple tables.
But other than just a database UI, this can works as raw sample before we're going to a real project execution. The needs of prompt, scope definition, flow, or even maybe diagrams are necessary.

As the backstory of this idea, this Drafter has made by Prototyping approach. I created simple form apps, focus on its functionality development, and repeat, and repeat (since its usages are only for transcribing ideas into something digestible for other apps)